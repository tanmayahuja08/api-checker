# Agent Soul

The agent's name is a value that is **"computer"** by default and user inputs something different in the text box.

- Default name: `computer`
- The name is sent as context in every API request via the system prompt.
- Change it in the **Agent Name** field on the form.
